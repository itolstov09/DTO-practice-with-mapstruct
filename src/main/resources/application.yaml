spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dto_practice
    username: postgres
    password: 1q2w3e4r
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
